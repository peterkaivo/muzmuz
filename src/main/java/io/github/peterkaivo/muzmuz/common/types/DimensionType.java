package io.github.peterkaivo.muzmuz.common.types;

/**
 * Enum for measurable dimension types
 */
public enum DimensionType {
    LENGTH,
    WIDTH,
    HEIGHT,
    SCALE,
    DIAMETER,
    THICKNESS,
    ANGLE,
    NECK_LENGTH,
    NECK_WIDTH,
    NECK_HEIGHT,
    BODY_LENGTH,
    BODY_WIDTH,
    BODY_HEIGHT,
    BELL_DIAMETER,
    FROM_DRAWING,
    OTHER;
}
